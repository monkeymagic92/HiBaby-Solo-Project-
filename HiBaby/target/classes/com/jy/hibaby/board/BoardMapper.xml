<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jy.hibaby.board.BoardMapper">


						<!-- select -->
	<!-- 판매글 등록할경우 주소값 지정을위해 이다음 i_board 값의 최대값을 받아옴 -->
	<select id="maxI_board" resultType="Integer">
		SELECT MAX(i_board) + 1 FROM t_board
	</select>
	
						
	<!-- 게시글 뿌리기 -->
	<select id="selBoard" resultType="BoardDMI">
		SELECT A.i_board, A.title, A.ctnt, B.nick, A.r_dt FROM t_board A
		LEFT JOIN t_user B
		ON A.i_user = B.i_user
	</select>						
						
						<!-- insert -->
						
	<!-- 판매글 등록하기 -->
	<insert id="insBoard">
		insert into t_board
		(i_user, title, ctnt, image_1, image_2, image_3, image_4, image_5)
		VALUES
		(${i_user}, '${title}', '${ctnt}', 
		<foreach collection="imageFileList" item="item" separator=",">
			'${item.imageFile}'
		</foreach>
		)
	</insert>					
						
						
						<!-- update -->
						
	<!-- 게시글 사진 수정 -->
	<update id="updImage">
		UPDATE t_board
		SET
		<choose>
			<when test='imageFile != null'>
				image_#{seq} = #{imageFile}
			</when>
		</choose>
		WHERE i_board = #{i_board}
	</update>			
	
	
	<update id="updBoard">
		UPDATE t_board
		SET title = #{title},
		ctnt = #{ctnt}
		WHERE i_board = #{i_board}
	</update>				
						
						<!-- delete -->

 	<!-- 게시글 삭제 -->
	<delete id="delBoard" parameterType="BoardPARAM">
		DELETE FROM t_board
		WHERE i_board = #{i_board}
	</delete>


	
</mapper>